Zoo Authentication System

Program prompts the user for their username and password and authenticates the credentials by comparing the user input to existing credentials. It then gives access to the txt file associated to that users role. For example, if the users role is admin, it gives access to the admin.txt file.
